DROP TABLE IF EXISTS tx_transaksi_cicilan_tetap;

CREATE TABLE tx_transaksi_cicilan_tetap(
    total_nilai_taksiran  
    nilai_pencairan_pelanggan 
    diskon_adm_buka 
)WITH(
    OIDS = FALSE
);

SELECT * FROM tx_transaksi_cicilan_tetap;